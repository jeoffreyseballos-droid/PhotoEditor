{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Local main window; native folder selection only. No shell, network, or arbitrary filesystem plugin.",
  "windows": ["main"],
  "permissions": ["dialog:allow-open"]
}
